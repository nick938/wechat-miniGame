# 目标：《摸鱼保卫战》好玩度与广告收入迭代规约

> 用途：直接粘贴到 ZCode 目标模式，作为自主迭代的唯一规约。
> 商业目标：**留存更好 → 更多人愿意连开几局、第二天回来 → 自愿多看广告 → 广告收入提升。**

## 0. 起点与现状（执行前先读）

- 仓库：微信小游戏《摸鱼保卫战》。4 武器 / 5 普通敌人 + 1 Boss（产品经理）/ 8 命名关 + 无尽 / 19 技能（白蓝紫橙）/ 局外工位三条升级线 / 4 处激励视频（复活 `revive`、结算双倍 `double`、技能刷新 `reroll`、每日宝箱 `chest`）。
- 单机、零依赖、纯 Canvas 2D + wx API。`node test/smoke.js` 64 项全过（固定随机种子，可复现）。
- 数值集中在 `js/core/config.js`；工位伤害统一入口 `js/systems/damage.js`；埋点 `js/services/track.js`；广告 `js/services/ads.js`。
- 已知缺口：棋盘满且无可合成对时没有出路、Boss 只有一种、无每日任务/成就、分享入口不全、线上广告加载失败会白送奖励、无前后台处理、若干性能可优化点。
- 商业路径：**个人主体 + 纯广告变现 + 免版号**。收入 ≈ DAU × 人均日观看次数 × eCPM。**你只能改"人均自愿观看次数"和"留存/裂变的代理指标"**，DAU 本身取决于分享裂变、平台推荐与外部流程（软著/备案），不要假装能保证。

## 1. 目标

把游戏改造成"愿意连开几局、有理由第二天回来、并自愿看广告"的产品。所有改进必须用**可复现的数据与测试**自证；拿不出证据的改动视为没做。

迭代方式：一轮一个主题，独立提交，可回退。

## 2. 硬约束（违反即本轮失败，必须回退）

**合规红线**
- 不加任何付费/内购/虚拟支付/礼包/会员；不接第三方广告 SDK，只用微信官方激励视频。
- 不引入登录、账号、自有服务器、云开发、第三方统计 SDK；不上传任何用户数据。
- 不做强制广告（不看广告就不能开始/继续）、不做插屏/开屏/Banner；不做"分享得奖励/集赞"（微信禁止诱导分享）。
- 失败时永远保留"不看广告"的出路（现有"认命，下班"按钮必须保留）。
- 文案保持安全化（如"带薪如厕"）；不新增擦边、侵权、涉政内容。

**技术红线**
- 保持零依赖：不加 npm 依赖、不加构建工具链、不重写引擎、不改目录结构（`js/core|entities|systems|ui|services`）、不动 `legacy/`。
- 不改 `game.json` 里 `"openDataContext": "openDataContext"` 的字符串写法（改成对象形式会被 IDE 校验拒绝）；保留打包排除项（`.mimosa/`、`.git`）。
- 主包远低于微信 4MB 上限：不新增大体积图片/音频，美术优先用代码绘制，图标类资源沿用 `tools/gen_icon.py` 的生成方式。

**工程纪律**
- `node test/smoke.js` 必须始终全绿且固定种子可复现；**不允许删除、跳过或放宽已有断言来"通过"**。
- 新增机制必须有测试；**新增测试必须反向验证**（把改动退回 → 用例必须失败），否则视为无效测试。
- 每轮只改一个主题；改坏就回退，不留半成品。
- 数值只改 `js/core/config.js`，不要散落进 UI。改频控/数值要同步 README、`上线资料.md`、`玩法说明.md` 的相关描述。
- 每轮结束提交（中文，写清"改了什么 + 指标怎么变"）；不 force push。

## 3. P0 自评基建（必须先做，否则后面全是拍脑袋）

1. 把 `test/smoke.js` 的 wx/canvas 桩抽成 `test/harness.js`，测试与模拟器共用；**禁止复制一份"模拟专用逻辑"**。
2. 新建 `tools/sim.js`：加载**真实游戏代码**（`js/main.js`），用启发式机器人跑 N 局（默认 200 局，可指定 seed），输出指标表（stdout）并落盘 JSON。
   - 机器人**必须走游戏的触摸入口**操作（与真人同一路径），不得直接改内部状态或绕过玩法。
   - 支持 `--seed` 复现单局日志，便于定位数值问题。
   - 至少三档水平：新手 / 普通 / 熟练，避免只对一种打法调参。
3. 指标至少覆盖：
   - 节奏：首杀、首次合成、首次三选一耗时；单局时长
   - 行为：每局合成次数、三选一出现次数、补给投放与丢弃次数
   - 结果：各关通过率、卡关分布、无尽中位关卡、失败原因分类（被啃死 / 时间到）
   - 挫败：满盘发生率、真死局（满盘且无可合成对）发生率及其持续时长
   - 经济：每局金币中位数、工位三线满级所需局数
   - 广告：每局可获得的自愿广告机会数、各 placement 触发次数
   - 压力：单帧最大敌人/弹丸/特效数量（性能代理）
4. `指标报告.md`：每轮追加"改动前 / 改动后"对比表 + 一句结论，**必须包含负向变化与不确定性**，只写好消息视为造假。
5. 顺手把当前未跟踪文件提交掉：`assets/`、`tools/gen_icon.py`、`上线资料.md`。

## 4. 改动方向（按优先级，做完前面再碰后面）

**P1 消除挫败**（性价比最高）
- 满盘无出路 → 加"回收/出售装备换金币"（长按或拖入回收区）+ 满盘与死局提示，保证真死局率为 0。
- 说清"我为什么输了"：谁啃掉最后一滴血、差多少、下次怎么改。

**P2 决策深度**（好玩的核心）
- 把纯数值叠加的技能改造成会改变打法的机制（连击、暴击、反弹、冰缓、连锁爆炸、装备专属词缀等），让三选一真的有取舍。
- 合成之外的局内决策：回收换钱、定向升星、Boss 前预警与站位取舍。
- 用模拟器统计"build 组合重复率"，避免每局都一样。

**P3 留存钩子**
- 每日任务 3 个（可用现有埋点驱动）+ 成就 6-10 个 + "差一点就成"的成长感。
- 图鉴/击杀统计（成本低、留存感强）；计划书 §14 的每日免费宝箱（非广告）。

**P4 广告价值**（收入直接杠杆，但必须自愿且不伤留存）
- 观看前明确告知能换到什么，观看后立即兑现（"值"要一眼看得出来）。
- 线上加载失败策略改为：重试若干次 → 仍失败则提示"广告暂时不可用"，**不发奖励**；只有 `AD_UNIT_ID` 为空（开发/未配置）时才走模拟广告。
- 现有频控不得放宽；新增广告点必须可跳过、且不影响通关。

**P5 分享裂变**（唯一不花钱的拉新渠道）
- 动态分享文案：去掉硬编码的"超过 92%"这类假数据，改用真实分数/关卡对比。
- 补 `wx.onShareAppMessage`（右上角转发也要带战绩）+ 5:4 分享卡片图（沿用 `tools/gen_icon.py` 的生成方式）。
- 好友榜可读性：名次、我的位置、可追赶的目标。

**P6 内容厚度**
- Boss 扩充（计划书 §10.2 直属领导 / §10.3 大老板），按章节或每 8 关轮换。
- 敌人与关卡主题化（章节名与敌人组合呼应）；计划书 §9.1 的「会议邀请」坦克敌人。
- 摸鱼分权重调整：让"守得好"和"推得远"都有份（当前剩余血量权重过低，苟着没收益）。

**P7 性能与手感**（不许拿性能换内容）
- `ctx.font` 缓存、每帧数组重建改原地回收、碰撞改 id→enemy Map、DPR 限幅 2、静态背景离屏缓存、特效上限统一。
- 处理前后台切换（`wx.onHide/onShow` 暂停 BGM 与计时）。
- 每轮改完复跑模拟器，确认单帧实体峰值不恶化。

## 5. 明确不要做

- 内购/虚拟支付/礼包/会员；登录与账号；服务器/云开发/云函数；**双人联机**（需要外部开通云环境与持续成本，留到 v1.1）。
- 第三方 SDK、H5 外链、跳转其他小程序、UGC 与聊天。
- 插屏/开屏/Banner、"看广告解锁关卡"类强制门槛、分享得奖励。
- 为提升指标而把难度降到毫无挑战，或靠堆敌人密度硬拖时长。
- 伪造/挑选指标、改断言或删用例来"达标"、把机器人调成不可能失败。

## 6. 每轮交付

1. 代码 + 测试（新增用例已反向验证）。
2. `指标报告.md` 新增一段：前后对比 + 结论（含负向变化与不确定性）。
3. 一句"为什么这样改"的设计理由（讲清为什么更好玩 / 为什么更愿意看广告）。
4. 一次提交（中文，含指标变化）。

## 7. 停止与汇报

- P1-P5 全部完成且指标达标 → 输出总结报告 + **《真机验证清单》**：要在开发者工具/真机上手动看什么、手感上感受什么、在微信后台「数据分析」看哪些数字（留存、分享率、人均广告观看、eCPM）。
- 连续两轮某方向没有正向收益 → 记录结论、跳过继续，不空转。
- 需要外部资源的（广告位 ID、云环境、软著/备案、名称占用）→ 停下来明确列出，不要绕道或伪造。

## 8. 初始量化目标（可依据模拟数据调整，调整必须写明理由）

- 真死局率 = 0；满盘时 100% 有提示与出路。
- 新手机器人：首杀 ≤ 12s、首次合成 ≤ 20s、首次三选一 ≤ 30s。
- 普通机器人：8 关通过率 40%–70%；无尽中位关卡 ≥ 12。
- 每局合成次数中位数 ≥ 15；每局三选一 ≥ 8 次。
- 每局自愿广告机会 ≥ 5 次（不含强制，且都不影响通关）。
- 工位三线满级总需局数 ≤ 25 局。
- 单帧最大实体数不超过改动前基线。
- 埋点完整：列出后台需创建的自定义事件清单并写进 `上线资料.md`（`level_start` / `level_end` / `skill_pick` / `weapon_merge` / `ad_show` / `ad_reward` + 本轮新增）。

## 9. 已知陷阱（踩过一次，别再踩）

- 键帽冲击波的命中判定**只比 x 不比 y**；改碰撞/攻击范围预览/测试时要一起改。
- 任何新增的工位伤害、减伤、护盾机制**必须走 `js/systems/damage.js`**，否则会出现"技能写了但没生效"（历史上两个技能就是这么废掉的）。
- 测试全绿 ≠ 数值生效：新增技能/词缀必须断言"数值真的变了"（例如实测掉血速率），不能只断言"技能进了列表"。
- 广告回调在真机上有概率不触发：`AdService.showing` 占用位配了 180s 看门狗，别改成永久占用或永久不占用。
- 模拟器若清场/改状态做隔离，注意棋盘武器会干扰（冲击波按 x 判定会秒掉整列），连锁类测试先清空 `board.cells` 与在飞的弹丸。
