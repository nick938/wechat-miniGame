# 摸鱼保卫战 🐟

微信小游戏 · 合成 + 塔防 + 肉鸽三选一 · 纯广告变现（无内购）

> 上班是不可能认真上班的。合成装备，抵御老板，守住你的摸鱼时间。

## 玩法

- 你扮演打工人，守住工位：敌人（Bug、群消息、紧急需求、产品需求…）从屏幕上方压向工位
- 底部 4×4 棋盘即防线：**拖动两个相同装备合成升级**（LV1→LV5），装备自动攻击
- 击杀得经验 → 升级弹 **三选一技能**（白/蓝/紫/橙 四档品质，共 19 个）
- 每 4 关一个 Boss（产品经理：疯狂加需求、残血狂暴）；8 关后进入无尽模式
- 每隔一段时间投放新装备补给；金币用于局外「工位升级」（显示器/人体工学椅/摸鱼学）
- 战斗右上角 **×1/×2/×3 倍速快进**（子步进模拟防穿模，倍速偏好自动存档，新手教学阶段隐藏）
- **摸鱼分**（击杀×10 + 关卡×1000 + 剩余血量×5，公式在 `config.js calcScore`）与**好友摸鱼榜**（开放数据域读好友托管数据，免服务器；结算自动上报，首页 🏆 查看，开发者工具内显示本地兜底视图）

**新手引导**（计划书 §16 落地）：**首次进入首页自动弹出 3 页图文玩法说明**（含合成/战场示意图、翻页圆点，`helpSeen` 存档后不再打扰，右上角 ❓ 可随时重看）；第 1 关开局冻结刷怪，高亮两个咖啡 + 手指沿虚线轨迹演示拖拽，玩家完成第一次合成后敌人慢 3 秒才进场；首次三选一有提示条。三步引导各自独立存档（`helpSeen` / `tutorialDone` / `skillTipDone`），老玩家不会被重复打扰。

## 运行

1. 用微信开发者工具「导入项目」选择本目录（appid 已配置）
2. 直接编译即可玩。未配置广告位时，看广告走 3 秒「模拟广告」倒计时降级，奖励照发

### 无头冒烟测试（Node，不需要开发者工具）

```bash
node test/smoke.js
# 期望输出：31 通过 / 0 失败（固定随机种子，结果可复现）
```

覆盖：进关 → 合成 → 刷怪击杀 → 三选一 → 暂停 → 胜利 → 双倍奖励（模拟广告）→ 下一关 → 失败 → 复活弹层 → 结算 → 工位升级 → 存档持久化。

## 配置广告位（上线前必做）

`js/core/config.js`：

```js
const AD_UNIT_ID = ''; // 填入微信后台创建的激励视频广告位 ID
```

激励视频广告位共 4 处（代码里已埋好，配置 ID 即生效）：

| 场景 | placement | 频控 |
|---|---|---|
| 死亡复活（恢复 50% + 击退敌人） | `revive` | 每局 1 次 |
| 结算金币翻倍 | `double` | 每次结算 1 次 |
| 三选一技能刷新 | `reroll` | 每局 2 次 |
| 首页每日宝箱 | `chest` | 每天 3 次 |

## 调参指南

所有数值集中在 `js/core/config.js`（数据驱动，改完即生效）：

- `WEAPONS`：4 种武器各级伤害/间隔/范围
- `ENEMIES`：敌人血量/速度/啃工位伤害/经验/金币
- `buildLevel(n)`：关卡时长、血量倍率、刷怪节奏公式（改难度曲线只动这里）
- `SKILLS` / `RARITY_WEIGHT`：技能效果与白蓝紫橙权重（55/30/12/3）
- `UPGRADES` / `UPGRADE_COST`：局外成长数值
- `expNeed`：经验曲线（当前刻意偏快，30 秒内迎来第一次三选一）

调试钩子（开发者工具控制台）：`databus.debug.addCoins(100)` / `addExp(200)` / `setTimeLeft(5)` / `giveWeapon('keyboard', 3)` / `killAll()` / `resetSave()`。

## 上线合规清单（个人主体、免版号、纯广告）

1. **软著**：申请《摸鱼保卫战》计算机软件著作权（普通通道约 1-2 个月，**和开发并行启动**，这是上线日期的决定因素）
   - 申报材料已生成在 `软著材料/`：源程序 PDF（56 页，总量不足 60 页按规范全量提交）+ 软件说明书 PDF（8 页）
   - 办理：身份证实名认证后到 [register.ccopyright.com.cn](https://register.ccopyright.com.cn) 在线填申请表并上传两份 PDF，免费
   - 申请表要与材料保持一致：软件全称 `摸鱼保卫战游戏软件`、版本 `V1.0`、开发完成日期 `2026-09-12`
   - 代码改动后重新生成材料：`python3 tools/gen_ruanzhu.py`
2. **小程序备案（ICP）**：微信公众平台 → 设置 → 基本设置 → 小程序备案（约 7-20 个工作日）
3. **自审自查报告**：自己写一份 PDF（游戏概况 + 内容自查结论 + 未成年人保护说明，模板网上搜），提审时上传
4. **提审**：微信开发者工具"上传" → 公众平台"版本管理 → 提交审核"，类目"游戏-休闲游戏"；个人主体**不要**出现任何付费/内购描述（本项目无支付代码，符合）
5. **文案**：已做安全化处理（"带薪拉屎"→"带薪如厕"）；语音梗注意版权，当前版本无语音
6. **流量主**：累计 1000 独立访客后开通；上线首周可用低预算投流（几分钱/UV）凑门槛
7. 微信后台创建激励视频广告位 → 填入 `AD_UNIT_ID`

## 技术结构

```
game.js                 入口
js/render.js            375×667 设计分辨率适配 + 触摸坐标换算
js/core/config.js       全部数值配置（数据驱动）
js/core/databus.js      全局状态：存档(meta) + 局内(battle) + debug 钩子
js/core/pool.js         对象池（敌人/弹丸/特效复用，防掉帧）
js/core/audio.js        音效（bgm/爆炸，可开关）
js/entities/            敌人（含 Boss 召唤/狂暴）、弹丸（咖啡豆/键帽波/Bug 炸弹）、特效
js/systems/             武器行为、技能抽取与词缀、波次控制
js/ui/                  战斗场景、合成棋盘、三选一、结算、首页、工位升级
js/services/            激励视频（含模拟降级）、分享
test/smoke.js           Node 无头冒烟测试（wx/canvas 桩）
legacy/                 原打飞机示例代码（已排除打包，仅留档）
```

关键设计决策：**棋盘即防线**——武器从棋盘格自动朝战场开火，无需角色移动，操作只剩拖拽合成与技能选择（计划书 §4.1 的"工位"方案）。

## 后续版本（v1.1+）

- 好友排行榜（开放数据域/云开发，需要云环境配置）
- 更多角色（程序员/设计师/测试…各带被动）、每日任务、成就系统
- 音效语音扩充、无尽模式排行榜赛季
