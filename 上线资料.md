# 《摸鱼保卫战》小游戏上线资料

> 更新：2026-09-12 ｜ 用途：微信公众平台后台「设置」与提审表单逐项填写
> 代码实际用到的微信能力（已核对）：本地存储 `wx.setStorageSync`、开放数据域 `wx.setUserCloudStorage` / `wx.getFriendCloudStorage`、分享 `wx.shareAppMessage`。**没有** wx.login、没有自有服务器、没有虚拟支付。

---

## 1. 头像

**推荐上传：`assets/icon/icon-v1-512.png`**（正方形 PNG，512×512，圆角透明底，微信要求 ≥144×144、≤2MB，均满足）

三个方案（`assets/icon/` 下各有 512/144 两档，拼图对比见 `preview-all.png`）：

| 方案 | 文件 | 画面 | 适合 |
|---|---|---|---|
| **v1 躺平摸鱼（推荐）** | icon-v1-512.png | 蓝底，闭眼躺平的橙鱼抱咖啡，Zzz，底部淡合成棋盘格 | 元素最少、小尺寸辨识度最高，「摸鱼」语义最直白；蓝色在微信列表里跳眼 |
| v2 工位守卫 | icon-v2-512.png | 青底，鱼在工位掩体后向逃跑的 Bug 发射咖啡豆 | 更能讲「保卫战」的故事，但 144px 下细节略糊 |
| v3 咸鱼躺平 | icon-v3-512.png | 暖黄底，灰蓝咸鱼 X 眼吐舌 | 梗味最重，适合当备用/活动图 |

想调整颜色、姿势：改 `tools/gen_icon.py`（所有坐标按 512 空间标注）后重跑 `python3 tools/gen_icon.py` 即可。

## 2. 名称

- **主选：摸鱼保卫战**
- 若被占用，按序备选：**打工人保卫战 → 守住我的鱼 → 带薪摸鱼大作战**
- 规则：4~30 字符、全平台唯一；名称需与实际功能相符（休闲塔防，无风险）。

## 3. 简介（后台限 120 字，本稿 40 字）

> 上班是不可能认真上班的！合成装备抵御老板，3 分钟一局随时开摸，和好友比比摸鱼分！

备选极简版（31 字）：

> 上班是不可能认真上班的。合成装备，抵御老板，守住你的摸鱼时间。

主稿保留搜索关键词：摸鱼、合成、老板、好友。

## 4. 服务类目

**游戏 → 休闲游戏**（个人主体可选范围）。

个人小游戏资质三件套（均与认证费无关）：
1. 《计算机软件著作权登记证书》——材料在 `软著材料/`
2. 游戏自审自查报告
3. ICP 备案（微信公众平台内提交，免费）

> 提醒：个人主体小游戏不支持微信认证、不能开虚拟支付，那 30 元/年认证费只存在于「小程序」，这边一分钱不用花，变现走流量主广告。

## 5. 用户隐私保护指引（后台逐条填写）

| 收集/使用 | 用途 | 说明 |
|---|---|---|
| 微信昵称、头像 | 好友排行榜（总榜 + 本周榜）展示 | 通过微信**开放数据域（关系链数据）**获取，仅在客户端开放数据域内渲染，**不经过、不存储于任何自有服务器** |
| 游戏进度、最高分（本地存储） | 保存进度 | 仅存于用户设备本地（`wx.setStorageSync`） |

- 不收集：位置、通讯录、相册、手机号、openid 等（代码未调用 wx.login）。
- 不向第三方提供用户数据；无内嵌外部链接。
- 广告为微信官方激励视频组件，无需开发者额外声明。

## 6. 数据分析：自定义事件清单（后台「数据分析 → 自定义事件」按名字创建）

代码里已埋好（`js/services/track.js`，未创建时静默跳过，不影响游戏）：

| 事件名 | 上报时机 | 参数 |
|---|---|---|
| `level_start` | 进入某一关 | `level` |
| `level_end` | 过关/失败结算 | `level` `win` `kills` |
| `skill_pick` | 三选一选卡 | `id` `stacks` |
| `weapon_merge` | 合成成功 | `lv` `type` |
| `weapon_recycle` | 装备回收换金币 | `lv` `coins` |
| `board_full` | 棋盘满、空投无处可放 | `level` |
| `boss_warning` | Boss 出场前 8 秒预警 | `level` `sec` |
| `daily_open` | 打开每日任务面板 | `claimable` |
| `daily_claim` | 领取每日任务奖励 | `id` `coins` |
| `daily_chest` | 领取每日免费宝箱 | `coins` |
| `codex_open` | 打开成就与图鉴 | `claimable` |
| `achievement_claim` | 领取成就奖励 | `id` `coins` |
| `ad_show` | 触发激励视频 | `placement` |
| `ad_reward` | 拿到广告奖励 | `placement` |
| `ad_unavailable` | 激励视频加载失败（重试后仍不可用，不发奖励） | `placement` |
| `share` | 分享/转发（主动分享 from=button，右上角转发 from=menu） | `from` `score` |
| `rank_tab` | 好友榜切到总榜/本周榜 | `mode` |
| `star_ticket` | 打死 Boss 获得升星券 | `level` `tickets` |
| `weapon_starup` | 用升星券定向升级装备 | `lv` `type` |
| `hot_col` | 本波敌人集中从某列压进 | `col` |

> 上线后重点看：`level_start` 的关卡分布（卡在哪一关）、`ad_reward / ad_show` 的完成率、`ad_unavailable` 的次数、`weapon_merge` 的人均次数（核心操作是否被玩起来）、`share` 的次数与 `daily_open`/`codex_open`（回归钩子是否被点开）。

## 7. 提审版本描述（每次提审填写的"版本描述"）

> 《摸鱼保卫战》是一款办公室题材的休闲合成塔防小游戏。敌人（Bug、群消息、紧急需求、产品需求及 Boss"产品经理"）从屏幕上方分波进攻，玩家在底部棋盘拖动合成装备（咖啡、键盘、Bug、耳机）自动防御，升级时三选一随机技能，守住工位血条至倒计时结束即过关。
> 内容量：新手教学关、常规关卡、每 4 关 Boss 关、通过第 8 关后开放无尽模式、局外工位升级（显示器/座椅/摸鱼学三条线）、每日宝箱、战斗倍速切换。
> 变现说明：无任何付费点；仅接入微信官方激励视频广告（复活、结算金币翻倍、技能刷新、每日宝箱），全部由用户自愿触发，**无强制广告、无插屏广告**。
> 数据说明：游戏进度仅存本地；好友排行榜通过微信开放数据域展示好友昵称、头像与摸鱼分，不使用自有服务器，不上传用户数据；含分享战绩功能。无外部链接，无诱导分享。

## 8. 分享文案

- 带战绩（**文案只用真实数据**，不再写"超过 N%"这类编造的百分比）：`我在《摸鱼保卫战》拿到摸鱼分 {分数}（第 {关卡} 关），来比比谁更会摸鱼 👉`
- 还没分数时：`老板来了！我在第 {关卡} 关打退了 {击杀} 个需求，快来帮我守住工位 👉`
- 卡片图：`assets/share/card-500x400.png`（500×400，5:4，由 `python3 tools/gen_share_card.py` 生成）
- 主动分享按钮与**右上角菜单转发**共用同一份文案（`wx.onShareAppMessage` 已注册），两条路径都上报 `share` 事件（`from=button` / `from=menu`）

## 9. 上线前 checklist

- [ ] 后台上传头像（本文件第 1 节）
- [ ] 名称/简介/类目填写（第 2~4 节）
- [ ] 用户隐私保护指引（第 5 节）
- [ ] 软著 + 自审自查报告 + ICP 备案提交（软著材料在 `软著材料/`）
- [ ] 真机回归：iOS 高/低机型 + 安卓中低端机，目标 FPS≥50
- [ ] 上线后：累计独立访客 ≥1000 时开**流量主**（免费、无需认证），接入激励视频广告位
